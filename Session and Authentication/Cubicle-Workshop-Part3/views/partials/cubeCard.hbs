<div class="cube">
  <img class="cube" src="{{imageUrl}}" />
  <p class="name">{{name}}</p>
  <p><span>Difficulty level:</span> {{difficulty}}</p>
  <a class="btn" href="/details/{{_id}}">Details</a>
  {{#if isOwner}}
    <a href="/attach/accessory/{{_id}}" class="btn">Attach accessory</a>
  {{/if}}
</div>